import type { LucideIcon } from "lucide-react";
import { AlertCircle, LoaderCircle } from "lucide-react";
type Props={type:"loading"|"empty"|"error";title?:string;message?:string;icon?:LucideIcon;actionLabel?:string;onAction?:()=>void};
export function StatePanel({type,title,message,icon:Icon,actionLabel,onAction}:Props){const StateIcon=Icon??(type==="loading"?LoaderCircle:AlertCircle);return <div className="grid min-h-[60vh] place-items-center p-8 text-center"><div className="max-w-sm"><span className="mx-auto grid size-16 place-items-center rounded-2xl bg-black text-white shadow-xl dark:bg-white dark:text-black"><StateIcon className={type==="loading"?"animate-spin motion-reduce:animate-none":""} size={28}/></span><h2 className="mt-6 text-2xl font-semibold tracking-[-.03em]">{title??(type==="loading"?"Loading…":type==="empty"?"Nothing here yet":"Something went wrong")}</h2>{message?<p className="mt-2 text-sm leading-6 text-neutral-500">{message}</p>:null}{actionLabel&&onAction?<button className="mt-6 rounded-lg bg-[#ed111c] px-5 py-3 text-sm font-semibold text-white shadow-lg shadow-red-600/15 transition hover:-translate-y-0.5 motion-reduce:transition-none" onClick={onAction}>{actionLabel}</button>:null}</div></div>}
