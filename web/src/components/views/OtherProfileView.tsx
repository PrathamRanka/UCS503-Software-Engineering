import { useState } from "react";
import { ArrowRight, MessageCircle, MoreHorizontal, UserPlus } from "lucide-react";
import { useNavigate, useParams } from "react-router-dom";
import { exploreItems, people } from "../../data/mockData";
import { PageHeader } from "../ui/PageHeader";
import { StatePanel } from "../ui/StatePanel";
type Props={following:Set<string>;onFollow:(handle:string)=>void;onAction:(message:string)=>void};
export function OtherProfileView({following,onFollow,onAction}:Props){const {username}=useParams();const navigate=useNavigate();const person=people.find(item=>item.username===username);const [menu,setMenu]=useState(false);if(!person)return <StatePanel type="error" title="Person not found" message="This person is not in the campus directory." actionLabel="Explore people" onAction={()=>navigate("/people")}/>;const handle=`@${person.username}`;const connected=following.has(handle);return <section className="mx-auto min-h-screen w-full max-w-5xl"><PageHeader title={person.name} eyebrow="Campus profile" action={<button className="grid size-10 place-items-center rounded-full border border-black/10 dark:border-white/10" onClick={()=>setMenu(true)}><MoreHorizontal size={18}/></button>}/><div className="px-4 py-8 sm:px-8"><div className="grid gap-8 rounded-2xl border border-black/10 bg-white p-6 dark:border-white/10 dark:bg-white/[.035] sm:grid-cols-[150px_1fr] sm:p-9"><img className="size-32 rounded-2xl object-cover" src={person.avatar} alt={person.name}/><div><p className="text-[10px] font-bold uppercase tracking-[.18em] text-[#ed111c]">@{person.username}</p><h1 className="mt-2 text-4xl font-semibold tracking-[-.045em]">{person.name}</h1><p className="mt-3 text-sm text-neutral-500">{person.branch} · {person.year}</p><p className="mt-5 max-w-xl text-sm leading-6">{person.bio}</p><div className="mt-7 flex flex-wrap gap-2"><button className={`flex h-11 items-center gap-2 rounded-lg px-5 text-xs font-semibold ${connected?"bg-black text-white dark:bg-white dark:text-black":"bg-[#ed111c] text-white"}`} onClick={()=>onFollow(handle)}>{connected?<ArrowRight size={16}/>:<UserPlus size={16}/>} {connected?"Connected":"Connect"}</button><button className="flex h-11 items-center gap-2 rounded-lg border border-black/10 px-5 text-xs font-semibold dark:border-white/10" onClick={()=>navigate("/inbox")}><MessageCircle size={16}/>Message</button></div></div></div><div className="mt-10"><p className="text-[10px] font-bold uppercase tracking-[.18em] text-[#ed111c]">Recent work</p><h2 className="mt-2 text-2xl font-semibold tracking-[-.03em]">What they’ve contributed</h2><div className="mt-5 grid gap-3 sm:grid-cols-2 lg:grid-cols-3">{exploreItems.slice(0,6).map(item=><button className="group aspect-[4/3] overflow-hidden rounded-xl" key={item.id}><img className="size-full object-cover transition duration-500 group-hover:scale-[1.03] motion-reduce:transition-none" src={item.image} alt="Campus contribution"/></button>)}</div></div></div>{menu?<div className="fixed inset-0 z-50 grid place-items-center bg-black/70 p-4 backdrop-blur-sm" onMouseDown={()=>setMenu(false)}><section className="w-full max-w-sm overflow-hidden rounded-2xl bg-white text-center dark:bg-[#111113]" onMouseDown={e=>e.stopPropagation()}>{["Mute updates","Report profile","Block profile"].map(action=><button className={`w-full border-b border-black/10 p-4 text-sm font-semibold dark:border-white/10 ${action!=="Mute updates"?"text-red-500":""}`} onClick={()=>{onAction(`${action} selected`);setMenu(false)}} key={action}>{action}</button>)}<button className="w-full p-4 text-sm" onClick={()=>setMenu(false)}>Cancel</button></section></div>:null}</section>}
